import React from "react";
import {Form ,Row, Col,FormGroup, Input, Label} from "react-bootstrap";
import {EditProfile} from './EditProfile';

const UserPage = ({ classes }) => {
  return (
    <div>
    <EditProfile/>
</div>      
  );
};
export default UserPage;
